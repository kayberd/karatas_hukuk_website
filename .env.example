# Cookieless analytics (Plausible) — optional. Set to your domain to enable.
PUBLIC_PLAUSIBLE_DOMAIN=

# Contact form POST endpoint (serverless function or form service e.g. Formspree).
# Leave empty in dev; form will show a "not configured" notice instead of posting.
PUBLIC_FORM_ENDPOINT=
